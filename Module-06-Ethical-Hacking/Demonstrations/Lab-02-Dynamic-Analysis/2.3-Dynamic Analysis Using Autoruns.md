# Lab 2.3 — Dynamic Analysis Using Autoruns

## 🎯 Objective

Use Autoruns to examine programs and system components configured to start automatically when Windows starts or when a user logs in.

For this demonstration, the benign Windows executable `notepad.exe` is used as the analysis reference.

---

## 🧪 Requirements

- Windows analysis VM
- `notepad.exe`
- Autoruns
- Isolated malware-analysis environment

---

## ⚠️ Safety

This demonstration is performed in an isolated Windows analysis VM.

When analyzing actual malware, always use a clean virtual-machine snapshot and avoid modifying persistence mechanisms unless the activity is explicitly part of an authorized lab.

---

## 🖥️ Sample Used

```text
notepad.exe
```

Default location:

```text
C:\Windows\System32\notepad.exe
```

Verify the file exists:

```powershell
Test-Path C:\Windows\System32\notepad.exe
```

Expected result:

```text
True
```

---

## Step 1 — Launch Autoruns

Open Autoruns on the Windows analysis VM.

Autoruns displays applications and system components configured to start automatically.

It can show startup locations such as:

- Logon
- Explorer
- Scheduled Tasks
- Services
- Drivers
- Windows Services
- Office
- WMI

The exact categories displayed depend on the Windows version and Autoruns version.

---

## Step 2 — Examine the Logon Tab

Open the:

```text
Logon
```

tab.

This section contains programs configured to run when a user logs into Windows.

Examine fields such as:

- Entry
- Description
- Publisher
- Image Path
- Timestamp

---

## Step 3 — Examine Startup Entries

Look for applications configured to execute automatically.

Pay attention to:

- Program Name
- Image Path
- Publisher
- Startup Location

Record interesting entries actually observed:

```text
Entry: <observed entry>
Image Path: <observed path>
Publisher: <observed publisher>
Location: <observed startup location>
```

---

## Step 4 — Examine Other Persistence Locations

Autoruns provides multiple tabs for examining different automatic-start locations.

Check relevant tabs such as:

- Logon
- Scheduled Tasks
- Services
- Drivers
- WMI

Review the entries without making changes to the system.

---

## Step 5 — Search for the Sample

Use Autoruns' search functionality to search for:

```text
notepad.exe
```

If no result is found, record:

```text
notepad.exe: No Autoruns entry observed.
```

This is an expected and valid observation because the normal `notepad.exe` executable is not necessarily configured as a startup program.

---

## Step 6 — Examine Entry Details

Select an interesting entry and examine its details.

Look for:

- Image Path
- Command Line
- Startup Location
- Publisher
- Description

If available, use the context menu to inspect the file or entry location.

Do not modify or delete entries during this demonstration.

---

## Step 7 — Examine Timestamps

If Autoruns displays timestamp information, examine it.

Record:

```text
Timestamp: <observed timestamp>
```

A timestamp can help establish when a file or entry was created or modified, but it should not be treated as proof of malicious activity by itself.

---

## Step 8 — Record Observations

Create a summary of the Autoruns analysis:

| Property | Observation |
|---|---|
| Entry | \<observed entry\> |
| Publisher | \<observed publisher\> |
| Image Path | \<observed path\> |
| Startup Location | \<observed location\> |
| Timestamp | \<observed timestamp\> |
| notepad.exe Entry | \<observed / not observed\> |

Only document information actually observed during the demonstration.

---

## 🔍 Understanding the Results

Autoruns helps analysts identify potential automatic-execution locations:

```text
Windows Startup
      ↓
Autoruns
      ↓
Startup Locations
      ↓
Programs / Services / Tasks
      ↓
Potential Persistence
      ↓
Further Investigation
```

An unfamiliar startup entry may require further investigation, but an entry being unfamiliar does not automatically mean it is malicious.
