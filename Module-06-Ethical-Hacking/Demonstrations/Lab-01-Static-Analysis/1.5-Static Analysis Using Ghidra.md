# Lab 1.5 — Static Analysis Using Ghidra

## 🎯 Objective

Use Ghidra to perform deeper static analysis and reverse engineering of a Windows executable.

For this demonstration, the benign Windows executable `notepad.exe` is used as the analysis sample.

---

## 🧪 Requirements

- Windows analysis VM
- `notepad.exe`
- Ghidra
- Java Runtime Environment (if required by your Ghidra version)
- Isolated malware-analysis environment

---

## ⚠️ Safety

This demonstration uses the legitimate Windows `notepad.exe` executable.

When analyzing actual malware, always use an isolated virtual machine and take a clean snapshot before beginning the analysis.

---

## 🖥️ Sample Used

```text
notepad.exe

Default location:

C:\Windows\System32\notepad.exe

Verify the file exists:

Test-Path C:\Windows\System32\notepad.exe

Expected result:

True
Step 1 — Launch Ghidra

Open Ghidra on the Windows analysis VM.

Create a new project:

File → New Project

Select:

Non-Shared Project

Choose a suitable project location and provide a project name.

Example:

MalwareAnalysis
Step 2 — Import the Sample

Import:

C:\Windows\System32\notepad.exe

into the Ghidra project.

Ghidra will identify the executable format and architecture.

Step 3 — Open the Program

Double-click the imported notepad.exe file.

Ghidra will display the CodeBrowser.

If Ghidra asks whether to perform automatic analysis, select:

Yes

Keep the default analysis options unless a specific option is required.

Step 4 — Examine the Program Tree

In the Program Trees or Symbol Tree area, examine the available program components.

Look for categories such as:

Functions
Labels
Imports
Exports
Strings

These provide an overview of the executable's structure.

Step 5 — Examine Strings

Open the Defined Strings view.

You can access it through:

Window → Defined Strings

Review strings identified by Ghidra.

Look for potentially useful information such as:

File paths
DLL names
API names
Application text
Error messages
Registry-related strings
URLs

Only record strings actually observed during the analysis.

Step 6 — Examine Functions

Open the Functions section in the Symbol Tree.

Ghidra automatically identifies functions during analysis.

Select a function to examine its:

Function name
Address
Instructions
References

Record interesting functions observed during the demonstration.

Step 7 — Examine the Disassembly

Select a function and examine the Listing window.

Ghidra displays the machine instructions used by the executable.

You may see assembly instructions such as:

MOV
CALL
PUSH
POP
LEA
CMP
JMP

The exact instructions depend on the selected function and executable architecture.

Step 8 — Examine the Decompiler

Select a function and open the Decompiler window.

Ghidra attempts to translate machine instructions into C-like pseudocode.

The output may look similar to:

int function_name(...)
{
    ...
}

Use the decompiler to understand the logic of the selected function.

Decompiled code is an approximation of the original program logic and should be interpreted together with the assembly instructions.

Step 9 — Examine Function Calls

Inspect calls made by the selected function.

Look for:

Function Calls
API Calls
Cross References
Called Functions

Use the references shown by Ghidra to understand how different functions interact.

Step 10 — Examine the Entry Point

Locate the program's entry point.

Ghidra provides information about where execution begins.

Record:

Entry Point: <observed value>

You can then follow the code from the entry point into other functions.

Step 11 — Record Observations

Create a summary of the Ghidra analysis:

Property	Observation
File	notepad.exe
Architecture	<observed value>
Entry Point	<observed value>
Strings	<interesting observed strings>
Functions	<observed functions>
Imports	<observed imports>
Interesting Function	<function examined>
Decompiled Logic	<brief observation>

Only document information actually observed during the demonstration.

🔍 Understanding the Results

Ghidra allows the analysis to progress from executable structure to program logic:

notepad.exe
     ↓
Import into Ghidra
     ↓
Automatic Analysis
     ↓
Strings / Functions
     ↓
Disassembly
     ↓
Decompiler
     ↓
Program Logic

This makes Ghidra useful for deeper static analysis and reverse engineering.
