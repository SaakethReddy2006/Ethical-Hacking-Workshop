# Lab 1.4 — Static Analysis Using PE Explorer

## 🎯 Objective

Use PE Explorer to examine the internal structure and characteristics of a Windows executable during static malware analysis.

For this demonstration, the benign Windows executable `notepad.exe` is used as the analysis sample.

---

## 🧪 Requirements

- Windows analysis VM
- `notepad.exe`
- PE Explorer
- Isolated malware-analysis environment

---

## ⚠️ Safety

This demonstration uses the legitimate Windows `notepad.exe` executable.

When analyzing actual malware, always use an isolated virtual machine and take a clean snapshot before beginning the analysis.

---

## 🖥️ Sample Used

```text
notepad.exe
```

Default location:

```text
C:\Windows\System32\notepad.exe
```

Verify the file exists:

```powershell
Test-Path C:\Windows\System32\notepad.exe
```

Expected result:

```text
True
```

---

## Step 1 — Launch PE Explorer

Open PE Explorer on the Windows analysis VM.

PE Explorer is used to examine the internal structure of Windows Portable Executable (PE) files.

---

## Step 2 — Load the Sample

In PE Explorer, open:

```text
C:\Windows\System32\notepad.exe
```

The tool will parse the executable and display information about its PE structure.

---

## Step 3 — Examine the PE Headers

Navigate through the PE header information.

Look for details such as:

- DOS Header
- PE Signature
- Machine
- Number of Sections
- Characteristics
- Entry Point
- Image Base

Record the values displayed by PE Explorer:

```text
Machine: <observed value>
Number of Sections: <observed value>
Entry Point: <observed value>
Image Base: <observed value>
```

Do not assume the values. Record what PE Explorer displays for your Windows version.

---

## Step 4 — Examine PE Sections

Open the Sections or Section Headers information.

Look for sections such as:

- `.text`
- `.rdata`
- `.data`
- `.pdata`
- `.reloc`

Examine properties such as:

- Section Name
- Virtual Address
- Virtual Size
- Raw Size
- Characteristics

Record the sections actually displayed:

| Section | Virtual Size | Raw Size | Characteristics |
|---|---|---|---|
| \<observed\> | \<value\> | \<value\> | \<value\> |
| \<observed\> | \<value\> | \<value\> | \<value\> |

---

## Step 5 — Examine Imported DLLs

Navigate to the Import Table.

The Import Table contains external libraries used by the executable.

Look for:

- DLL Names
- Imported Functions
- Windows API Functions

Record the DLLs and functions displayed by PE Explorer:

```text
Imported DLLs:
<observed DLLs>

Imported Functions:
<observed functions>
```

Imports can provide clues about the functionality an executable may use.

---

## Step 6 — Examine Exports

Navigate to the Export Table, if available.

Exports represent functions or symbols made available by the executable to other programs.

Record the information displayed:

```text
Exports:
<observed information>
```

If no exports are displayed, record:

```text
Exports: None observed
```

---

## Step 7 — Examine Resources

Open the Resources section.

Look for resource types such as:

- Icons
- Dialogs
- Menus
- Version Information
- Manifest
- Embedded Resources

Record the resources observed:

```text
Resources:
<observed resource types>
```

Resources can provide additional information about an executable and its application interface.

---

## Step 8 — Record Observations

Create a summary of the PE Explorer analysis:

| Property | Observation |
|---|---|
| File | notepad.exe |
| Machine | \<observed value\> |
| Number of Sections | \<observed value\> |
| Entry Point | \<observed value\> |
| Image Base | \<observed value\> |
| Sections | \<observed sections\> |
| Imports | \<observed DLLs/functions\> |
| Exports | \<observed information\> |
| Resources | \<observed resources\> |

Only document information actually observed during the demonstration.

---

## 🔍 Understanding the Results

PE Explorer allows the executable to be examined at the PE-structure level:

```text
notepad.exe
     ↓
PE Headers
     ↓
Sections
     ↓
Imports / Exports
     ↓
Resources
     ↓
Static Analysis Findings
```

This provides more detailed structural information than simply viewing the executable as a normal Windows application.
